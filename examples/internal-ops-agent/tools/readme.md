# Tools

Add TypeScript tool modules here when the project needs executable helpers.

Keep the public contract file-first until tools are explicitly required. This example includes `create_ticket.tool.ts` as a safe dry-run smoke tool.
