# @plico/cli

CLI entrypoints for validating Plico projects.

## Commands

- `plico validate [path]`
- `plico validate --json [path]`

If no path is provided, the command validates the current working directory.
